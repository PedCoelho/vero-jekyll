<nav>
    {% for item in site.data.navigation %}
        <a href="{{item.link | relative_url}}" {% if item.link != "/" and page.url contains item.link %}class="current-page"{% endif %}>
            {{ item.name }}
        </a>
    {% endfor %}
</nav>